{
  "name": "do-016-task-dag-executor",
  "version": "0.1.0",
  "private": true,
  "description": "Manufactured implementation of DO-016 Task DAG Executor (L2). Executes an explicit task graph with per-step checkpoints, crash-resume, and log replay.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-transform-types --test 'test/*.test.ts'"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.7.0"
  }
}
